class Visita{
    constructor(tv, eff, pag, data, referto)
    {
        this.tipoVisita = tv;
        this.effettuata = eff;
        this.data = data;
        this.referto = referto;
    }
}

module.exports = Visita;