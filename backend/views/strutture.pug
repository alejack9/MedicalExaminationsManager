html
  head
    title Medical Examinations Manager
  body
    div
      each val in strutture
        a(href=`localhost:3000/prenotazioni/prenota/${val._id}`) #{val.nome} - #{val.tipiVisita[0].prezzo} €
        |
        br